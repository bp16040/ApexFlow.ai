# Operations

Docker Compose is optional and provided as a development/deployment placeholder. Local backend and frontend commands do not require Docker.

Use structured application logs via Python's standard logging configuration. Production deployments should inject secrets and environment-specific configuration through the platform secret manager rather than committing `.env` files.
